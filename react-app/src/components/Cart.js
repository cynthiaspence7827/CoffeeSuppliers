import React from 'react';
import { useSelector } from 'react-redux';

const Cart = (props) => {
    const cart = useSelector(state => state.cart);

    return (
        <>
        </>
    );
}

export default Cart;
